<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch - BagStore</title>
    <link rel="icon" type="image/svg+xml" href="/images/scatch_favicon.svg">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <link rel="stylesheet" href="/fonts/Roboto/stylesheet.css">
    <link rel="stylesheet" href="/stylesheets/output.css">
    <script src="/javascripts/hamburger.js" defer></script>
</head>

<body class="font-['Roboto']">

    <nav class="w-full flex justify-between items-center px-4 sm:px-6 md:px-8 py-4 sm:py-6 relative">

        <a href="/" class="flex items-center gap-2">
            <img src="/images/scatch_favicon.svg" alt="Scatch Logo" class="h-6 sm:h-8 w-auto">
            <span class="text-base sm:text-lg md:text-xl">Scatch - Bags and Beyond</span>
        </a>

        <!-- Admin Login Button - Only on home page when NOT logged in -->
        <% var isHomePage = typeof isHomePage !== 'undefined' ? isHomePage : false %>
        <% var loggedin = typeof loggedin !== 'undefined' ? loggedin : true %>
        <% var isAdmin = typeof isAdmin !== 'undefined' ? isAdmin : false %>

        <% if(isHomePage && !loggedin){ %>
            <a href="/owners/login" 
               class="px-3 sm:px-4 py-2 bg-zinc-800 text-white rounded-md hover:bg-zinc-700 transition-colors flex items-center gap-2 text-sm sm:text-base">
                <i class="ri-admin-line"></i>
                <span class="hidden sm:inline">Admin Login</span>
            </a>
        <% } %>

        <% if(loggedin) {%>

            <button id="hamburger" class="md:hidden flex flex-col gap-1.5 p-2 z-50">
                <span class="w-6 h-0.5 bg-black transition-all"></span>
                <span class="w-6 h-0.5 bg-black transition-all"></span>
                <span class="w-6 h-0.5 bg-black transition-all"></span>
            </button>

            <ul class="md:flex items-center gap-8 z-50 md:z-auto md:static absolute bg-white w-full left-0 md:w-auto md:py-0 py-4 md:pl-0 pl-7 md:mt-0 mt-10  opacity-0 md:opacity-100 top-[-400px] transition-all ease-in duration-500 shadow-lg md:shadow-none" id="nav-menu">
                
                <!-- Show Admin Badge if logged in as admin -->
                <% if(isAdmin) {%>
                    <li class="my-6 md:my-0">
                        <span class="px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-xs sm:text-sm font-medium">
                            <i class="ri-shield-star-line"></i> Admin
                        </span>
                    </li>
                <% }%>
                
                <li class="my-6 md:my-0">                
                    <a href="/shop" class="hover:text-blue-600 transition-colors">Shop</a>
                </li>
                
                <!-- Cart link - different for admin -->
                <% if(isAdmin) {%>
                    <li class="my-6 md:my-0">                   
                        <a href="/cart" class="flex items-center gap-1 hover:text-blue-600 transition-colors">
                            Cart <span class="text-xs text-gray-500">(View Only)</span>
                        </a>
                    </li>
                <% } else { %>
                    <li class="my-6 md:my-0">
                        <a href="/cart" class="hover:text-blue-600 transition-colors">Cart</a>
                    </li>
                <% }%>
                
                <li class="my-6 md:my-0">                
                    <a href="/orders" class="hover:text-blue-600 transition-colors">Orders</a>
                </li>

                <li class="my-6 md:my-0">
                    <a href="/account" class="hover:text-blue-600 transition-colors">My Account</a>
                </li>
                
                <li class="my-6 md:my-0">
                    <a class="text-red-600 hover:text-red-700 transition-colors" href="/users/logout">Logout</a>
                </li>

            </ul>

        <% }%>
    </nav>

    <div class="flex justify-center">
        <div class="w-full border-t border-zinc-200"></div>
    </div>